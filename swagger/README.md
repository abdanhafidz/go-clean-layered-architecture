# Swagger Quzuu Ready

1. Copy folder `swagger/` to your project root
2. Import `_ "your_module/swagger/docs"` in main.go
3. Run: swag init --parseDependency --parseInternal
4. Open: /swagger/index.html
